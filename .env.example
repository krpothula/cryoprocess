# =============================================================================
# CryoProcess - Configuration
# =============================================================================
# Copy this file to .env and edit for your environment:
#   cp .env.example .env
#
# Or run the install script which generates .env interactively:
#   ./cryoprocess.sh install
# =============================================================================

# =============================================================================
# 1. SERVER
# =============================================================================

PORT=8001
NODE_ENV=production
CORS_ORIGIN=*
LOG_LEVEL=info

# =============================================================================
# 2. DATABASE
# =============================================================================

MONGODB_URI=mongodb://localhost:27017/cryoprocess-db

# =============================================================================
# 3. AUTHENTICATION
# =============================================================================

# REQUIRED — generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=
JWT_EXPIRES_IN=7d

# =============================================================================
# 4. DATA PATH (Required — edit for your system)
# =============================================================================

ROOT_PATH=/path/to/your/data

# Archive / cold storage path (optional)
# ARCHIVE_PATH=/mnt/archive/projects

# =============================================================================
# 5. SLURM CLUSTER
# =============================================================================

SLURM_PARTITION=batch
SLURM_SUBMIT_COMMAND=sbatch
SLURM_POLL_INTERVAL=3000

# =============================================================================
# 6. SSH REMOTE CLUSTER (Optional)
# Enable if CryoProcess runs on a different machine than the SLURM cluster.
# Requires a shared filesystem (NFS/Lustre) for ROOT_PATH.
# =============================================================================

# SLURM_USE_SSH=false
# SLURM_SSH_HOST=cluster.example.com
# SLURM_SSH_USER=myuser
# SLURM_SSH_PORT=22
# SLURM_SSH_KEY_PATH=~/.ssh/id_rsa

# =============================================================================
# 7. SOFTWARE & CONTAINER
# =============================================================================

# Path to RELION singularity image (.sif file)
RELION_PATH=
SINGULARITY_BIND_PATHS=
SINGULARITY_OPTIONS=--nv

# CTF estimation
CTFFIND_EXE=
GCTF_EXE=

# Motion correction
MOTIONCOR2_EXE=

# Model building
MODELANGELO_EXE=

# =============================================================================
# 8. EMAIL NOTIFICATIONS (Optional)
# =============================================================================

# EMAIL_NOTIFICATIONS_ENABLED=false
# SMTP_HOST=smtp.youruni.edu
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=your-email@youruni.edu
# SMTP_PASS=your-password
# SMTP_FROM=CryoProcess <your-email@youruni.edu>

# =============================================================================
# 9. SMARTSCOPE INTEGRATION (Optional)
# =============================================================================

# SMARTSCOPE_API_KEY=

# =============================================================================
# 10. FRONTEND (Optional — only needed for custom WebSocket host/port)
# These are baked into the React build at compile time.
# =============================================================================

# REACT_APP_WS_HOST=localhost
# REACT_APP_WS_PORT=8001
